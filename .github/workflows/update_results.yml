name: Update Race Results

on:
  schedule:
    # Friday 22:00 UTC — catches FP1/FP2 once those are wired into fetch_results.
    # Until then it's a no-op (find_pending_rounds returns empty), which is fine.
    - cron: '0 22 * * 5'
    # Saturday 22:00 UTC — catches Saturday sessions once they are past
    # the 4h post-start buffer; late qualifying slots are picked up Sunday.
    - cron: '0 22 * * 6'
    # Sunday 17:00 UTC — catches 13:00 UTC race starts at the 4h post-start buffer.
    - cron: '0 17 * * 0'
    # Monday 22:00 UTC — catches late Sunday night races (Americas) + data availability buffer
    - cron: '0 22 * * 1'
    # Tuesday 22:00 UTC — catch-up for OpenF1 lag / rate limits after race weekend
    - cron: '0 22 * * 2'
  workflow_dispatch:
    inputs:
      force_rounds:
        description: 'Comma-separated round numbers to re-pull (e.g. 1,2). Leave blank to refresh every populated round (REFRESH_WINDOW cap is lifted on manual runs).'
        required: false
        default: ''

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  update-results:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Fetch results from OpenF1
        env:
          FORCE_ROUNDS: ${{ inputs.force_rounds }}
          EVENT_NAME:   ${{ github.event_name }}
        run: |
          # Manual runs (workflow_dispatch) without specific force_rounds
          # pass --refresh-all so populated sessions are re-pulled regardless
          # of REFRESH_WINDOW. The pre-fetch buffer still applies, so
          # in-progress and future sessions are still skipped. Cron runs
          # keep the default 48h cap so we don't pointlessly hit OpenF1
          # for every archived round on every scheduled run.
          if [ -n "$FORCE_ROUNDS" ]; then
            python scripts/fetch_results.py --force-rounds "$FORCE_ROUNDS"
          elif [ "$EVENT_NAME" = "workflow_dispatch" ]; then
            python scripts/fetch_results.py --refresh-all
          else
            python scripts/fetch_results.py
          fi

      - name: Update career wins in standings
        run: python scripts/update_standings.py

      - name: Commit and push if updated
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add f1_2026_results.json f1_2026_standings.json
          git diff --staged --quiet && echo "No changes." || (
            git commit -m "Auto-update results + career wins $(date -u '+%Y-%m-%d') [skip ci]"
            git push
          )
